# Tunnel Manager
## How to compile it
```
pyinstaller --noconfirm --onefile --windowed --name "TunnelManager" --icon=NONE tunnel_manager.py

```
## How to re-compile
```
pyinstaller --noconfirm --onefile --windowed --name "TunnelManager" --clean tunnel_manager.py
```